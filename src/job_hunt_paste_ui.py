"""DEPRECATED — dead module, scheduled for deletion (code review 2026-06-19).

Not imported anywhere in `src/` or `tests/`. Emptied during the post-refactor lint
sweep. The sandbox could not delete the file; run `git rm src/job_hunt_paste_ui.py`
to remove it for good.
"""
