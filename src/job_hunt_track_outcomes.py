"""DEPRECATED — dead module, scheduled for deletion (code review 2026-06-19).

Not imported anywhere in `src/` or `tests/`. The previous contents referenced an
undefined name `outcomes_store` (an unfinished `job_hunt_track_store` rename) and
would have raised `NameError` if ever called. Emptied to remove that landmine.

The sandbox could not delete the file; run `git rm src/job_hunt_track_outcomes.py`
to remove it for good.
"""
