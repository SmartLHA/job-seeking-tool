# Move swarm / multi-LLM-chat code out of the job-hunt repo — 2026-09-21

Mike approved: "把它整套移到 OpenClaw 專案" (2026-09-21). Finding: the live OpenClaw workspace (~/.openclaw/workspace) already has NEWER versions of these (src/shared_bus.py 125KB vs 10KB here; src/swarm_router.py 83KB vs viewer/swarm_router.py 28KB; own tests). The repo copies are older forks. So DO NOT copy into the live workspace (would clobber/confuse, and that dir is a dirty git repo with secrets history). Archive instead under ~/.openclaw/archive/ (move-never-delete; reversible).

## Steps
1. Create ~/.openclaw/archive/2026-09-job-hunt-swarm-fork/ and write MANIFEST.sha256 (sha256 of every file BEFORE moving) plus README.txt (what it is, origin repo path, date, "live newer versions exist in ~/.openclaw/workspace/src", how to restore = mv back per manifest).
2. MOVE (mv, keep relative paths under the archive dir): src/shared_bus.py; viewer/swarm_router.py; viewer/session_guard.py; viewer/swarm_before_after.html; viewer/task_ids.json; task_ids.json (root); swarm_pipeline.db (root, 0 bytes); tests/test_shared_bus_getters.py, tests/test_swarm_stage_derivation.py, tests/test_swarm_router_auto_advance.py, tests/test_session_guard.py, tests/test_multi_llm_chat.py; the whole viewer/conversations/ directory (189 saved chat threads, gitignored, may be private: move, DO NOT open or read them). Verify sha256 of each file at destination equals the manifest, and the source is gone.
3. Clean viewer/viewer_server.py (project copy) so it no longer imports or serves: shared_bus, swarm_router (import ~:42, initialize_swarm_router ~:3370-3372), swarm status/cleanup routes, session_guard, and the multi-LLM chat handlers/routes (~:46-53, 1430-1600, 1870-1975; find by grep swarm|shared_bus|session_guard|multi_llm|conversations|minimax). KEEP everything job-hunt related (/api/url-parse, /api/text-parse, /api/job-submit and its adapter, kanban, docs viewer, etc.) and everything else that isn't swarm/chat. Remove the code cleanly (no dead stubs), and drop now-unused imports/constants. Do not read ~/.openclaw/openclaw.json contents; if the server reads it for the removed features only, that code goes too.
4. Docs: update INDEX.md (test table rows for the 5 moved tests, module rows for shared_bus, mentions at ~:124,167-168), PROJECT_TODO.md QW-4 entry (record the move + archive path), docs/function_list.md if it mentions them, and docs/audits/ui-functions-audit-2026-09-19.md (note item 10 resolved 2026-09-21). Add a dated line, don't rewrite history.

## Acceptance criteria
- Manifest check prints OK for all moved files; `git status` shows the moved tracked files as deleted and nothing else unexpected; conversations dir gone from repo.
- `grep -rniE "shared_bus|swarm_router|session_guard|swarm" src viewer tests *.py *.sh` returns only intentional mentions (state each remaining hit and why).
- `python3 -c "import ast;ast.parse(open('viewer/viewer_server.py').read())"` OK, and the project viewer starts: run it with the venv on a SCRATCH port (NOT 8765), using scratch dirs if it has options, curl its root page and /api/text-parse with a tiny JSON body, then kill that process only (by the PID you started). It must not touch ~/.openclaw or port 8765.
- `venv/bin/python -m pytest tests/ -q`: 0 failed (expected count drops by the 44 moved tests: about 1064 - 44 = 1020 passed).
- No file under ~/.openclaw/workspace or ~/.openclaw/openclaw.json touched (record `git -C ~/.openclaw/workspace status --short | md5` before/after).
- Back up every edited repo file to backups/ (timestamped). Do not commit.
